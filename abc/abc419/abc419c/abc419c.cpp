#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main(){
       long long n;
       cin >> n;
       vector<long long> r(n),c(n);
       for(int i=0;i < n;i++){
            cin >> r[i] >> c[i];
       }

}

