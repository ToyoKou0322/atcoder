#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main(){
    long long h,w,n;
    cin >> h >> w >> n;
    vector<long long> a(n),b(n);
    for(int i=0;i < n;i++){
        cin >> a[i] >> b[i];
    }


}

