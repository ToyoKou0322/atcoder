#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main(){
    long long n,q;
    cin >> n >> q;
    vector<long long> a(n),b(q);
    for(int i=0;i < n;i++){
        cin >> a[i];
    }
    for(int i=0;i < q;i++){
        cin >> b[i];
    }


}

