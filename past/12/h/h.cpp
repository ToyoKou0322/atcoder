#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main(){
    int n,x;
    cin >> n >> x;
    vector<long long> a(n),b(n),c(n);
    for(int i=0;i < n;i++){
        cin >> a[i] >> b[i] >> c[i];
    }


}

